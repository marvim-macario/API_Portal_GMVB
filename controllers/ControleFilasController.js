const {fila_acompanhamento, fila_pendencia, fila_portabilidade, fila_regularizacao, fila_total3, fila_confirmacao, fila_digitacao, fila_preanalise, fila_precadastro, fila_saldo} = require('../models');

const Sequelize = require('sequelize');
const Op = Sequelize.Op

const ControleFilasController = {
    
    
}

module.exports = ControleFilasController;